# デザインシステム

資料作成支援アプリが生成するスライドの、見た目に関するルールを1ファイルにまとめたもの。
`js/theme.js` の1箇所を単一ソースとして、プレビュー（HTML/CSS）と PowerPoint 書き出し
（PptxGenJS）の両方が同じ値を参照する設計になっている。新しいデザインパターンを追加する
ときは、まずこのファイルの原則に沿っているかを確認する。

## 出自

NRI（野村総合研究所）が経済産業省向けに作成した報告書PDF（2024FY/000700, 2023FY/000283）
から、実際に使われている色・書体・余白をピクセル単位で抽出し、そのまま採用している。
「官公庁向け報告書」に見られる、情報量が多く・華美な装飾を避け・意思決定に必要な論点を
淡々と積み上げていくトーンを基準にしている。

## デザイン原則

1. **単一ソース・多重出力**：色・フォント・箇条書き記号は `DocAssist.theme` の1オブジェクトに集約する。プレビュー（CSS変数）とPPTX書き出し（PptxGenJS options）は、このオブジェクトを毎回読み直すだけで、値の受け渡しロジックを持たない。テンプレート取り込み機能や出力時アレンジも、この一箇所を書き換えるだけで全パターンに反映される。
2. **色は意味を運ぶ、装飾ではない**：オレンジ・レッドは「本当に一点だけ強調したい」ときの予約色（★推奨マーカー、影響度「高」など）。それ以外は青の階調とグレーだけで階層・順序・対比を表現する。多用すると「本当に重要な1点」が埋もれるため、意図的に制限している。
3. **結論を先に（ピラミッド原則）**：全パターン共通のヘッダー層に、そのスライドの結論・示唆を一文で言い切るメッセージを置く。ボディはその根拠を示す図表であり、メッセージの繰り返しにしない。
4. **本文は必ず見出し・ラベルを持つ**：スライドいっぱいを無地の箇条書きで埋める場面は実務上ほとんど無い。文章中心のパターンでも、各行に「ラベル：本文」の構造か、無ければ自動採番した見出しを必ず添える（詳細は[本文の必ずラベル化](#本文は必ず見出しラベルを持つ本文レイアウト規約)）。
5. **情報密度を優先し、装飾は最小限に**：角丸はごくわずか（`ROUND = 0.04in`）、罫線は髪の毛のように細く（`HAIRLINE = 0.75pt`）、番号は塗りつぶさず輪郭の丸で示す。「デザインのための余白」より「読める情報量」を優先する。
6. **プレビューと出力の一致**：HTMLプレビューはPowerPointの縮小コピーではなく別レンダリングエンジンだが、スライド寸法・余白の比率、フォント最小サイズ、色の参照元は完全に一致させる。プレビューだけの独自ルールを持たない（唯一の例外は「出力時アレンジ」＝意図的にプレビューへ反映しない設計、後述）。

## カラーシステム

### パレット

| トークン | 既定値 | 用途 |
|---|---|---|
| `primary` | `#000F78` ネイビー | 基本色。タイトルバー・見出し文字・番号・矢印・罫線 |
| `primaryLight` | `#3C64AA` ミディアムブルー | 第2階調。補助的な強調・2番目の系列 |
| `mid` | `#6C8CC9` 中間ブルー | 第3階調。円弧矢印・入れ子シェブロンの外帯 |
| `accent` | `#7B9BC7` ライトブルー | 第4階調。表・チャートの塗り分け |
| `softBlue` | `#9DB4DC` ソフトブルー | 第5階調。段階的に薄くする帯の中間 |
| `accent2` | `#5A63A7` 青紫 | 補助テキスト・軽い強調 |
| `gray` | `#8C97A6` グレー | 「他と違う／注意」を色相を変えずに示す（オレンジ・レッドの代替） |
| `highlight` | `#F59637` オレンジ | **予約色**。★推奨マーカーなど、本当に一点だけ強調したい箇所 |
| `critical` | `#C0392B` レッド | **予約色**。リスク・警告など、より強い注意喚起 |
| `positive` | `#000F78`（primaryと同値） | 増加・強み・機会の意味づけ |
| `negative` | `#8C97A6`（grayと同値） | 減少・弱み・脅威の意味づけ（オレンジ・レッドは使わない） |
| `light` | `#D6DFF0` | コンテンツボックスの塗り |
| `lighter` | `#EDF1FA` | テーブルの縞模様、淡い背景 |
| `pale` | `#F5F7FC` | 象限の塗り分けなど最も淡い階調 |
| `paleGray` | `#ECECEC` | 象限で1つだけ色相を外したいとき |
| `border` | `#BFBFBF` | 罫線（控えめに使用） |
| `text` | `#000000` | 本文 |
| `subtext` | `#595959` | 出所表記・ページ番号等の小さい文字 |
| `white` | `#FFFFFF` | 濃色背景上の文字 |

### 7段階ブルースケールの理由

入れ子シェブロン、段階的に濃くなる帯、円弧サイクルなど、同系色を並べて階層・順序を表す
図では、隣り合う段が識別できるだけの階調数が要る。`primary → primaryLight → mid → accent
→ softBlue` の順に淡くなる5段（+ `light`/`lighter`/`pale` の背景用3段）を用意し、
`scaleColor(theme, i, n)` ヘルパーで「i番目 / 全n段」から自動的に該当色を引けるようにしている。

### 予約色ルール（最重要）

- オレンジ（`highlight`）・レッド（`critical`）は、**1スライドにつき本当に強調したい1点だけ**に使う。
- 比較パターンでの★推奨マーカー、進捗表の「遅延」、影響度「高」など、あらかじめ決まった少数の意味にのみ紐付ける。新しいパターンで「目立たせたいから」という理由だけでこの2色を使わない。
- テンプレート取り込み機能（後述）でも、取り込み元テーマのアクセント2〜6（赤やオレンジを含みうる）は一切反映しない。取り込むのはアクセント1（primaryのみ）とし、そこから7段階を機械的に再生成する。これにより、どんなテンプレートを読み込んでも予約色の意味づけは壊れない。

### 状態色（進捗・評価バッジ）

| 状態 | 色 | 判定語の例 |
|---|---|---|
| 完了 | `primary`（ネイビー） | 完了、済、Done、クローズ |
| 進行中 | `primaryLight`（ミディアムブルー） | 進行、対応中、着手、WIP |
| 未着手 | `gray`（グレー） | それ以外 |
| 遅延・高リスク | `critical`（レッド） | 遅延、遅れ、超過、危険、影響度「高」 |

「遅延」だけが例外的にレッドを使う唯一の状態語（本当に注意喚起が必要な箇所のみ予約色を使う方針の一貫）。

## タイポグラフィ

- **フォント**：既定は `Yu Gothic UI`。`DocAssist.theme.fontFace` を単一ソースとし、テンプレート取り込み時は取り込み元の本文フォント（和文指定を優先）に差し替わる。
- **最小フォントサイズ**：PowerPoint書き出し時、本文として読めるテキストは**12pt以上**になるよう自動的に底上げされる（`js/pptxExport.js` の `withDefaultFont` が全ての `addText`/`addTable` 呼び出しを横断してチェックする単一箇所での実装）。新しいパターンで9pt等の詰め込みサイズを指定しても、この下限は自動的に効く。グラフの軸ラベル・凡例などPowerPointネイティブのグラフ機能が持つ内部フォントはこの対象外。
- **見出し文（メッセージ）**：20pt、太字、ネイビー。テンプレートからタイトルレイアウトを取り込んでいる場合はそのフォントサイズを使う。
- **箇条書き本文**：12〜14pt程度が目安（パターンにより変動）。
- **強調（インライン）**：`**強調したい語句**` と書くと、その部分だけ太字＋ネイビーになる。ネイビーの帯など色つき背景の上では白文字が沈まないよう、太字のみになり色は変えない（`.pv-bullets .em` 等、背景が淡い要素だけ明示的にネイビー化するクラスを個別指定している）。

## レイアウト・グリッド

### スライド寸法

A4横向き（297mm × 210mm ＝ 11.69in × 8.27in）。左右余白 `MARGIN_X = 0.55in`。

### 4層構造（全パターン共通）

すべてのデザインパターンは次の4層構造に従う。①②④は `js/pptxExport.js` が共通描画し、
パターンごとの実装は③のボディだけを担当する（＝新しいパターンを追加してもヘッダー・
フッターは自動的に統一される）。

| 層 | 内容 | 主な寸法 |
|---|---|---|
| ① ヘッダー | 紺の縦棒（`BAR_W=0.07in`）＋パンくず形式の小見出し（`｜`区切りで複数セグメント） | `HEADER_Y=0.28in` から、高さ`CRUMB_H=0.26in` |
| ② メッセージ | 結論を言い切る太字ネイビーの見出し文＋任意のサブメッセージ、直下にヘアライン罫線 | 1行`0.42in`／2行`0.72in`（34文字超で2行判定） |
| ③ ボディ | 各パターン固有の図・表・チャート | ヘッダー下、余白`BODY_GAP=0.16in`＋内側パディング`0.05in` |
| ④ フッター | 左下に出所表記（あれば）、右下にページ番号 | 高さ`FOOTER_H=0.3in` |

見出し文に `｜` を含めると「（１）調査の背景｜①課題認識」のように複数セグメントの
パンくずとして扱われる。箇条書きに `出所：◯◯` と書くと、その行は取り除かれ左下の
出所表記に自動移動する。

### タイトルレイアウトの取り込み

既存.pptxのスライドマスターにタイトルプレースホルダーがある場合、その位置・サイズ・
フォントサイズを比率変換して①②の座標にそのまま反映できる（`theme.titleLayout`）。
4層構造そのものは崩さず、「タイトル」に相当する要素だけ位置の自由度を許している。

### 共通スペーシングトークン

| トークン | 値 | 用途 |
|---|---|---|
| `ROUND` | `0.04in` | コンテンツボックスの角丸（帯・シェブロン・表には使わない） |
| `HAIRLINE` | `0.75pt` | 標準の罫線太さ |
| `HAIRLINE_THIN` | `0.5pt` | 補助的な罫線（目盛り・区切り） |
| `BODY_PADDING` | `0.05in` | ボディ外枠から実コンテンツまでの内側余白 |

## コンポーネント語彙

同じ意味の要素は、どのパターンでも同じ見た目になるよう共通ヘルパーに切り出している。

- **箇条書きマーカー**：既定は `■`（`theme.bulletChar`、テンプレート取り込みで差し替え可）。チェックマーク`✓`は「確認済み・完了」の意味に予約（チェックリスト、背景→課題→対策の各パターン）。丸囲み数字`①②③…`はラベルの無い項目への自動見出し。
- **番号**：塗りつぶさず輪郭だけの円（`addNumberCircle`）。塗りつぶしバッジより軽く見え、淡い背景の上でも沈まない。
- **矢印**：文字の「→」ではなく小さな三角形（`triangle` シェイプ、90度回転）で統一。フロー間の区切り、課題→対応案の変化を示す箇所などで共通利用。
- **アイコン**：画像を貼り込まず、PowerPoint標準の図形（`flowChartDocument`、`flowChartMagneticDisk`、`cloud`、`gear6` 等）で描く。書き出し後もPowerPoint上で色・サイズを直接編集できる。語句からアイコン種別を推測する `ICON_HINTS`（例：「クラウド」→雲、「DB」→データベース）を用意し、パターン側で明示指定しなくても自動選択される。
- **表**：見出し行はネイビー塗り＋白太字、偶数行に薄い縞模様（`lighter`）。強調行は薄い塗り＋ハイライト色の上下罫線。
- **フラッグ／タブ形状**：`homePlate` シェイプ（右端が矢印状にとがった五角形）を、見出しバーの装飾やシェブロンフローの起点に使う。
- **チェックボックス**：確認済みは塗りつぶし＋白いチェックマーク、未確認は白地にネイビー枠線のみの四角。

## 記法・シンタックス規約

パターンをまたいで共通の区切り記号を使うことで、ユーザーが記法を覚え直さずに複数の
パターンを行き来できるようにしている。

| 記号 | 意味 | 例 |
|---|---|---|
| `：` | 項目名と内容の区切り（全パターン共通） | `A案：低コストだが拡張性に課題` |
| `｜` | 列・段階の区切り | `要件定義：山田｜完了｜9/10` |
| `／` | 1つのグループ内の並列要素 | `As-Is：基幹システム／顧客DB` |
| `（）` | 直前の要素の補足行 | `顧客DB（Oracle）` |
| `★` `★推奨` | 推奨・注目項目のハイライト（予約色オレンジ） | `B案：★推奨 バランスが良い` |
| 行末の `：`（内容なし） | 小見出し行（小見出し＋箇条書き系パターン） | `背景：` |
| 行頭の `-` | サブ項目（ノート風見出し／前回の振り返り×本日の討議内容の左側） | `-サーバー保守費用` |
| `---` のみの行 | 左右パネルの区切り（前回の振り返り×本日の討議内容） | 3文字以上の `-`/`＝` だけの行 |
| `**強調**` | インライン強調（太字＋ネイビー） | `**重要な語句**を含む文` |

これらはすべて `js/analyze.js` の共通ヘルパー（`splitKV`、`extractItems`、
`parseEmphasisTokens` 等）で解析される。新しい記号を増やす前に、既存の記号で表現できないか
を優先的に検討する。

### 本文は必ず見出し・ラベルを持つ（本文レイアウト規約）

スライドいっぱいを無地の箇条書きで埋める場面はほとんど無いという前提に立ち、文章中心の
フォールバックパターン（タイトル＋メッセージ、2列箇条書き）でも本文の各行に見出しを
強制する。

- 「ラベル：本文」の行 → ラベルをそのまま太字見出しにする。
- ラベルの無い行（コロン無しの文章） → 丸囲み数字（①②③…、9個を超えると「10.」形式）を自動採番して見出し代わりにする。

`A.extractItems` は強調記法（`**bold**`）を除去してしまうため使わず、生の行に対して
`A.splitKV` を直接使うことで、ラベル・本文どちらでも `**強調**` を保持できるようにしている
（`splitLabelValue` / `labeledListRuns` / `inlineLabeledBulletRuns`）。

## パターンライブラリ

`js/patterns.js` に73種類、9カテゴリで定義されている（2026年8月時点）。

### カテゴリ別内訳

| カテゴリ | 件数 |
|---|---|
| 汎用 | 16 |
| 構造・ロジック | 12 |
| フロー・プロセス | 9 |
| 比較 | 8 |
| 時系列 | 7 |
| マトリクス／ポジショニング | 6 |
| 変化・対比 | 5 |
| アクションプラン | 5 |
| 数値・グラフ | 5 |

### 役割（role）別内訳

見た目の形式で分類する軸（用途で分類する`category`とは別軸）。テンプレートギャラリーの
絞り込みに使う。

| role | 件数 | 例 |
|---|---|---|
| 図解 | 30 | フロー、マトリクス、ピラミッド、組織図 等 |
| 本文 | 10 | タイトル＋メッセージ、小見出し＋箇条書き 等 |
| 表 | 9 | 比較表、汎用表、課題・リスク一覧 等 |
| 計画 | 8 | ガント、アクションプラン、タイムライン 等 |
| KPI | 6 | KPIサマリー、各種チャート 等 |
| 比較 | 6 | ボックス比較、評価マトリクス 等 |
| 目次 | 4 | アジェンダ、中扉 等 |

### シーン（scenes）

`提案書` `進捗MTG` `報告書` `キックオフ` `経営・定例報告` `構成図` `汎用ロジック図解`
`データ提示` `研修・説明会` `会社・採用` の10種類。テンプレートギャラリーの絞り込み軸。

### パターンオブジェクトの形

```js
{
  id: 'kebab-case-id',
  name: '表示名',
  category: '比較',          // 用途分類（9種類から選ぶ）
  description: '説明文',
  score(section) { ... },    // {heading, bullets} を見て0〜10で採点
  renderBody(bullets) { ... },              // プレビュー用HTML
  buildBody(slide, bullets, theme, box) { ... }, // PptxGenJSでのPPTX組み立て
}
```

`score()` は複数パターンが同じ入力に反応しうる前提で設計する（例：小見出し＋箇条書きの
4バリエーションは、いずれも `parseHeadingGroups` の同じ入力を解釈できるが、スコアを
7〜9の範囲でわずかに変えて優先順位をつけつつ、テンプレートギャラリーからはいつでも
手動で切り替えられる）。最高得点のパターンが自動選択されるだけで、他の対応可能な
パターンが排除されるわけではない。

## テーマの拡張性

### テンプレートからの色・フォント・タイトルレイアウト取り込み

既存のPowerPointファイルを読み込み、`ppt/theme/theme1.xml`（テーマカラー・フォント）、
`ppt/slideMasters/slideMaster1.xml`（箇条書き記号・タイトルプレースホルダー）、
`ppt/presentation.xml`（スライド寸法）を解析して、次の4点をこのアプリに反映する
（`js/pptxImport.js`）。

- 基調色（アクセント1、無ければ濃色2）→ 7段階ブルースケールを機械的に再生成
- 本文フォント（和文指定を優先）
- 箇条書き記号（記号専用フォント指定時は文字化けを避け既定の■にフォールバック）
- タイトルプレースホルダーの位置・サイズ・フォントサイズ

取り込まないのはタイトル以外のプレースホルダー配置、accent2〜6（予約色を守るため）。

### 出力時の自動アレンジ

ベースのデザインパターン自体は変えず、PowerPoint書き出しのタイミングだけで、章（見出しの
先頭パンくずセグメント）ごとに基調色の濃淡をわずかに変化させ、資料全体で3種類の見出しバー
装飾（四角／角丸／ドット付き）から1つを選ぶ（`js/pptxExport.js` の `arrangedTheme`）。
資料タイトル・章構成から決まる再現性のある値で、ランダムではない。オレンジ・レッド等の
予約色や本文色には一切触れない。意図的にプレビュー画面には反映しない（書き出したファイル
にのみ反映）。

## アーキテクチャ上の設計判断

- **色は毎回読み直す**：`DocAssist.theme` は不変オブジェクトではなく、プロパティを
  上書き・復元される可変オブジェクト。パターンの `buildBody`/`renderBody` は呼び出しの
  たびにこれを読み直すため、テーマ切り替えに伴うイベント通知や再レンダリングの仕組みが
  不要になっている。
- **CSS変数がプレビューの反応点**：HTMLプレビューの色・フォント・箇条書き記号は
  `var(--primary)` 等のCSSカスタムプロパティ経由で参照する。テーマが変わったときは
  `applyCssVars()` でルート要素のプロパティを書き換えるだけで、全パターンのプレビューが
  再レンダリングなしに追従する。
- **PPTX書き出しの共通ラッパー**：`withDefaultFont(slide)` が `slide.addText`/`addTable`
  を差し替え、フォント統一・最小サイズ底上げをパターン実装から隠蔽する。個々のパターンは
  フォントサイズや色の下限を意識せずに実装できる。
- **プレビューとPPTXの二重実装**：各パターンは `renderBody`（HTML文字列）と `buildBody`
  （PptxGenJS呼び出し）を別々に持つ。見た目のロジックを完全に共有することはできないが、
  同じ配色・同じ記法解釈ヘルパー（`js/analyze.js`）を両方から参照することで実質的な一致を
  保っている。

## 新しいパターンを追加するときのチェックリスト

1. 色は `theme.primary` 等の変数経由で参照しているか（ハードコードした16進値を書いていないか）
2. オレンジ・レッド（`highlight`/`critical`）を、既存の意味づけ（★推奨、遅延、影響度「高」）以外の目的で使っていないか
3. フォントサイズは12pt以上を意識しているか（下回っても書き出し時に自動で底上げされるが、レイアウト設計時点で意識する）
4. 本文が「見出しの無い無地の箇条書き」になっていないか（ラベル：本文、または自動採番のどちらかを経由しているか）
5. 既存の記号（`：｜／（）★`）で表現できないか、新しい記号を増やす前に確認したか
6. `category`（用途）と `role`（見た目の形式）の両方を `PATTERN_META` に登録したか
7. ギャラリー用のサンプルデータ（`SAMPLES`）を追加したか
8. `renderBody` と `buildBody` の両方を実装し、見た目が一致しているか
